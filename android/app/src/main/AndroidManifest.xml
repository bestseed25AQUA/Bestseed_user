<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declare package & intent visibility so UrlLauncher can resolve external apps on Android 11+ -->
    <queries>
        <!-- WhatsApp (regular and Business) -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />

        <!-- Resolve web and whatsapp scheme intents -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="api.whatsapp.com" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>

        <!-- Phone call intent (required for canLaunchUrl with tel: scheme on Android 11+) -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>

        <!-- Play Store, for "Rate us" / "Share app". The explicit package is
             what lets MainActivity target Play Store directly instead of the
             system showing a "pick a store" chooser on devices with more than
             one app store installed. -->
        <package android:name="com.android.vending" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>

        <!-- Any browser / https link handler — needed on Android 11+ for the
             https fallbacks (store listing, privacy policy, report links) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>

        <!-- Share sheet targets (ACTION_SEND) for "Share app" -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- READ_EXTERNAL_STORAGE was only needed by legacy image_picker paths on
         Android 12 and below (capped via maxSdkVersion). On Android 13+ we
         rely on the Android Photo Picker (ActivityResultContracts.PickVisualMedia)
         that image_picker 1.x uses by default — the picker runs in a separate
         process and returns a URI, so the app itself never needs
         READ_MEDIA_IMAGES / READ_MEDIA_VIDEO. Play Store rejected an earlier
         build for declaring READ_MEDIA_IMAGES unnecessarily. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- CALL_PHONE and SEND_SMS removed. The app never uses SmsManager or
         directly places a call — every "call vendor" / "message vendor"
         action goes through url_launcher with a tel: or WhatsApp URI,
         which opens the OS dialer / WhatsApp app for the user to confirm
         and requires no permission. Play Store rejected an earlier build
         for declaring these sensitive permissions without a matching core
         functionality (Anti-SMS Phishing / Default SMS handler etc.). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- MANAGE_EXTERNAL_STORAGE removed to comply with Play Store policy.
         The PDF report download in FarmTankListScreen now writes to the
         app-owned external files directory (no permission required) and
         opens the source URL in the browser instead of the local file. -->

    <application
        android:enableOnBackInvokedCallback="true"
        android:label="Bestseed"
        android:name="${applicationName}"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">

        <!-- Google Maps API Key — injected by build.gradle.kts from the
             gitignored secrets.json (or the GOOGLE_MAPS_API_KEY env var on CI).
             Never hard-code the key here. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />    

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                   <action android:name="android.intent.action.VIEW"/>
                   <category android:name="android.intent.category.DEFAULT"/>
                   <category android:name="android.intent.category.BROWSABLE"/>
                   <data android:scheme="myapp" />   
         </intent-filter>


        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Disable Impeller to fix image decoding failures on OnePlus/OxygenOS devices -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
</manifest>
